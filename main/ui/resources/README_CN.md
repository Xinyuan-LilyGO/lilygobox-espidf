# UI 编译资源

本目录保存已经转换为 C/C++ 数据、会直接编译进固件的 UI 资源。

- `fonts/`：LVGL 文本字体、图标字体、字体清单和生成工具。
- `images/`：启动器应用图片、图片清单、声明和生成工具。

工程根目录的 `assets/` 保存 TTF 等原始素材，是转换工具的输入；本目录保存
转换后的固件资源，是 ESP-IDF 的编译输入。不要手工修改自动生成的字体或
图片数组，应修改原始素材后重新生成。
